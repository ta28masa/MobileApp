import React　from 'react';
import {
    Text,
  } from 'react-native';

const NewScreen = ({navigation}) => {
    return(
        <React.Fragment>
            <Text>Hi there ! This is a new screen!</Text>
        </React.Fragment>
    );
}

export default NewScreen;